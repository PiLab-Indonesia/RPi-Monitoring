# Dashboard (Manager) — RPi-NMS

This documents the React Dashboard and how managers can use it.

## Start
1. Ensure backend is accessible (default: http://localhost:8000)
2. From repo root:

cd frontend
npm install
npm start

